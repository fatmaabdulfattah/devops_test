apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ayfolder
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - host: example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ayfolder
                port:
                  number: 8000